{
  "ConnectionStrings": {
    "DefaultConnection": "Server=${MYSQLHOST},${MYSQLPORT};Database=${MYSQL_DATABASE};User=${MYSQLUSER};Password=${MYSQLPASSWORD};"
  },
  "Authentication": {
    "Facebook": {
      "AppId": "${FACEBOOK_APP_ID}",
      "AppSecret": "${FACEBOOK_APP_SECRET}"
    },
    "Google": {
      "ClientId": "${GOOGLE_CLIENT_ID}",
      "ClientSecret": "${GOOGLE_CLIENT_SECRET}"
    }
  },
  "GoogleReCaptcha": {
    "Key": "${GOOGLE_RECAPTCHA_KEY}",
    "Secret": "${GOOGLE_RECAPTCHA_SECRET}"
  },
  "SendGrid": {
    "ApiKey": "${SENDGRID_API_KEY}"
  },
  "AllowedHosts": "*"
}
